# Ретро — 2026-04-03 (пятница)

## Метрики

| Метрика | Значение |
|---|---|
| Коммитов за окно | 12 |
| Файлов затронуто | 741 (большинство — бинарные ассеты figma-assets) |
| Основной объём | Figma DS (скиллы, компоненты, ассеты), замена emoji на AssetPlaceholderView, фикс CDO warnings, admin DS preview |

## Коммиты

1. **`575b1a6` figma(ds): add 4 missing components** — AssetPlaceholder, StatPointsBadge(3), LowHPPotionBanner(2), OpponentIntegratedCard; чистка дубликатов Payout Pill
2. **`27f8fa7` fix(ds): replace emoji fallbacks with AssetPlaceholderView + fix CDO warnings** — основной коммит дня: 29 Swift-файлов обновлены, новый компонент AssetPlaceholderView, рефакторинг DarkFantasyTheme и LayoutConstants, добавлены BGM-треки и звуки
3. **`0e13efb` fix(admin): stance token names zoneHead/zoneChest** — фикс имён токенов стоек в админ DS preview
4. **`ccc45bf` chore: remove hexbound-site** — перенос лендинга в отдельный репо, добавление DS Figma скиллов
5. **`a3e1e09` chore: Figma skills, asset export scripts, HTML prototypes** — массовый импорт Figma-скиллов и 333 файлов figma-assets
6. **`5ab977b` docs: design system audit, guild spec, retros** — обновление документации
7. **`15cc98e` feat(backend+admin): fortune wheel status, social messages fix** — бэкенд фичи
8. **`098272c` feat(ios/assets): fortune wheel art + shop gold tier icons** — новые арт-ассеты
9. **`d4b90c8` feat(sfx): expanded SFX library (70+ variants) + BGM tracks** — звуковая библиотека
10. **`04de806` feat(ios): UI polish across all screens** — масштабный UI-полиш
11. **`2794f5e` feat(ios): new components (CardLevelBadge, PvPStatsWidget, TreasureRewardOverlay)** — новые компоненты
12. **`c89bf65` feat(ios/theme): update design system tokens, card styles, motion constants** — обновление дизайн-системы

## Паттерны и уроки

### 1. pbxproj.bak закоммичен в проект

В коммите `27f8fa7` закоммичен файл `Hexbound/Hexbound.xcodeproj/project.pbxproj.bak` (2082 строки). Это junk-файл, который не должен быть в репо. Аналогично `admin/src/lib/design-tokens.json.backup`. Сканер `gather_metrics.sh` уже ловит `*.bak`/`*.backup` файлы (отображает 69 junk), но **gatekeeper** не блокирует коммит при их наличии.

**Действие:** Нужно добавить `.bak` и `.backup` в `.gitignore` и удалить закоммиченные файлы.

### 2. Bare `.textPrimary` без префикса `DarkFantasyTheme` — 8 мест

Метрики показали 2 "unsafe" bare token — при проверке найдено 8 мест, где `.textPrimary` используется без полного префикса `DarkFantasyTheme.textPrimary`. Работает благодаря Color extension, но нарушает конвенцию проекта.

**Файлы:** PremiumPurchaseView, VictoryParticlesView (2×), DesignSystemPreview (2×), ArenaDetailView, ArenaComparisonSheet, HubEditorDetailView.

### 3. HTML-прототипы и markdown в корне проекта

В корне репо лежат 4 HTML-прототипа (`fortune-wheel-prototype.html`, `social_buttons_prototype.html` и т.д.) и файл `HEXBOUND_FIGMA_DESIGN_SYSTEM_TOKENS.md`. Это не production-файлы и засоряют корень.

**Действие:** Перенести прототипы в `docs/prototypes/` или `User/prototypes/`, токен-файл — в `docs/07_ui_ux/`.

### 4. Массовый импорт Figma-скиллов — 20+ новых файлов

За день добавлены скиллы: `cc-figma-component`, `cc-figma-tokens`, `edit-figma-design`, `figma-code-connect`, `figma-create-design-system-rules`, `figma-generate-design`, `figma-generate-library`, `figma-implement-design`, `figma-use`, `apply-design-system`, `audit-design-system`, `fix-design-system-finding`, `rad-spacing`, `remove-background`. Это существенное расширение инструментария для Figma↔Code синхронизации.

### 5. AssetPlaceholderView — новый паттерн замены emoji

Коммит `27f8fa7` ввёл `AssetPlaceholderView.swift` как замену emoji-фоллбэкам в 25+ вьюхах. Это правильный паттерн, но сканер guardian не проверяет наличие оставшихся emoji в Views/.

## Правила/агенты обновлены

Обновлений агентов в этом ретро не делается — все найденные проблемы документированы как задачи на следующий ретро.

## Нарушения (snapshot)

| Проверка | Статус |
|---|---|
| `Color(hex:)` в Views/ | ✅ 0 |
| Merge conflict markers | ✅ 0 |
| Prisma schema sync | ✅ in sync |
| Bare tokens (unsafe) | ⚠️ 2–8 (`.textPrimary` без DarkFantasyTheme) |
| Junk files (.bak/.backup) | ❌ 2 файла в репо |
| HTML protoypes в корне | ⚠️ 4 файла |

## Задачи на следующий ретро

- [ ] Удалить `project.pbxproj.bak` и `design-tokens.json.backup` из репо, добавить в `.gitignore`
- [ ] Исправить 8 мест с bare `.textPrimary` → `DarkFantasyTheme.textPrimary`
- [ ] Перенести HTML-прототипы из корня в `docs/prototypes/`
- [ ] Перенести `HEXBOUND_FIGMA_DESIGN_SYSTEM_TOKENS.md` в `docs/`
- [ ] Добавить в guardian/scanner проверку на emoji-фоллбэки в Views/ (grep для распространённых emoji)
- [ ] Добавить в gatekeeper проверку на `*.bak`/`*.backup` файлы перед коммитом
