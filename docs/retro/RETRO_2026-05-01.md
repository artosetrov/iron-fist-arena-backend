# Ретро — 2026-05-01 (Пятница)

## Метрики

| Метрика | Значение |
|---|---|
| Коммитов за 24ч | 0 |
| Файлов затронуто (committed) | 0 |
| Последний коммит | `6b11996` (2026-04-29) — уже разобран в `RETRO_2026-04-30.md` |

Активности в git за окно 2026-04-30 → 2026-05-01 нет. Новых правил не требуется.

## Состояние рабочего дерева (uncommitted, для контекста)

Рабочее дерево содержит изменения, уже описанные в `RETRO_2026-04-30.md` §4 (Combat V2 scaffold removal): удалены 7 файлов в `Hexbound/Hexbound/Views/Combat/V2/`, плюс правки в `CombatViewModel`, `CombatEngine`, `BattlePreloader`, `BattleSummaryView`, `CombatDetailView`, `InteractiveBattleView`, `AppRouter`, `AppState`, `pbxproj` и три SKILL.md (`guardian` / `mirror` / `oracle`). Это **тот же diff**, что наблюдался вчера, без новых сюрпризов: V2-каркас уже демонтирован, а следующий combat-редизайн стартует с чистой базы, а не поверх старого scaffold.

## Правила/агенты обновлены

Ничего. Тихий день.

## Открытые задачи (перенесены из RETRO_2026-04-30 §4 — статус актуален)

- [ ] **D-1 / D-5 переносы из удалённого V2-каркаса** — rating delta+total в `RewardsBlock` и `confirmationDialog` для SKIP'а должны быть подняты в общие компоненты или новый combat-редизайн до того, как uncommitted-удаление V2 уйдёт в `main`. Иначе уроки растворятся вместе с каркасом.
- [ ] **Закоммитить или откатить uncommitted diff** — рабочее дерево "висит" уже >24ч с момента создания ретро 04-30. Если редизайн стартует не сразу, как минимум стоит зафиксировать удаление V2 отдельным коммитом, чтобы `git log` отражал решение.
