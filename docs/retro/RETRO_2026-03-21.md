# Ретро — 2026-03-21 (суббота)

## Метрики

| Метрика | Значение |
|---|---|
| Коммитов за день | 7 |
| Файлов затронуто | ~518 |
| Строк добавлено | ~36 106 |
| Строк удалено | ~3 420 |
| Merge-конфликты | Были, все разрешены |
| Оставшиеся `<<<<<<<` маркеры | 0 |

## Что было сделано

1. **Большой merge `origin/main`** — слияние с основной веткой, ~25+ конфликтов (коммит `14bb557`). Потребовалась отдельная фиксация оставшихся маркеров (`9857b97`).
2. **Очистка ассетов** — переименование файлов с пробелами, удаление дубликатов (`Contents 2.json`, `Localizable 2.strings`), перенос пользовательских файлов в `User/`.
3. **Фикс bare color tokens** — замена `.textPrimary` → `DarkFantasyTheme.textPrimary` в `UnifiedHeroWidget` и `DungeonMapEditorView` (коммит `42bf9f5`).
4. **Новые правила в CLAUDE.md** — merge conflict resolution, color token shorthand, gitignore `tsbuildinfo`.
5. **Фикс async closure** — `ErrorStateView.loadFailed` обёрнут в `Task {}` (коммит `eac7471`).
6. **Создание 7 агентов** — blacksmith, chronicler, gatekeeper, guardian, herald, mirror, oracle со скриптами.
7. **Pre-deploy аудит** — фиксы `await prisma.$disconnect()`, переименование BGM файлов, обновление pbxproj.

## Уроки

### 1. Bare color tokens — неполная фиксация (Scanner False Negative)

**Проблема:** Коммит `42bf9f5` исправил bare `.textPrimary` только в 2 файлах (`UnifiedHeroWidget`, `DungeonMapEditorView`), но **26 нарушений осталось** в 12 других файлах: `CombatDetailView`, `LoginView`, `WelcomeView`, `ShopOfferBannerView`, `PremiumPurchaseView`, `CurrencyPurchaseView`, `DungeonRoomDetailView`, `HeroIntegratedCard`, `HPBarView`, `AppearanceEditorDetailView`, `ItemCardView`, `DesignSystemPreview`, `HeroDetailView`, `HubEditorDetailView`.

**Вывод:** Правило добавлено в CLAUDE.md, но сканер `guardian` не проверяет все файлы или не запускался. Нужно убедиться, что `check_design_system.sh` ловит `.foregroundStyle(.textPrimary)` и аналогичные паттерны.

**Действие:** Обновить guardian-сканер для обнаружения bare tokens.

### 2. Merge-конфликты требуют обязательной проверки (Recurring Manual Fix)

**Проблема:** После merge (`14bb557`) потребовался отдельный коммит для удаления оставшихся `<<<<<<<` маркеров в 3 файлах. Это уже зафиксировано как инцидент в CLAUDE.md.

**Вывод:** Правило работает — было создано после инцидента. Gatekeeper-сканер должен проверять маркеры в preflight.

### 3. Файлы с пробелами в именах (Recurring Pattern)

**Проблема:** Множество файлов с пробелами (`Contents 2.json`, `Hub bg 2.jpg`, `Localizable 2.strings`) — дубликаты macOS. Удалены, но паттерн повторяется.

**Вывод:** Правило «no spaces in filenames» уже в CLAUDE.md. Blacksmith-сканер должен это проверять.

### 4. Нет ретро-скилла

**Проблема:** Scheduled task ссылается на `.skills/skills/hexbound-retro/SKILL.md` и `gather_metrics.sh`, которых не существует.

**Действие:** Скрипт метрик находится в `.skills/skills/chronicler/scripts/gather_metrics.sh`. Нужно обновить scheduled task или создать алиас.

## Текущие нарушения (Open Items)

| Тип | Кол-во | Файлы |
|---|---|---|
| Bare `.foregroundStyle(.textPrimary)` | 26 | 12 файлов (см. выше) |
| `Color(hex:)` в комментариях | 1 | `CityMapEffects.swift` (не критично) |
| Merge markers `<<<<<<<` | 0 | Чисто |

## Правила/агенты обновлены

- Нет обновлений агентов в этом ретро (уроки задокументированы для следующей сессии).

## Задачи на следующее ретро

1. **Исправить оставшиеся 26 bare token нарушений** — массовый find-and-replace `.foregroundStyle(.textPrimary)` → `.foregroundStyle(DarkFantasyTheme.textPrimary)` в 12 файлах.
2. **Обновить guardian scanner** — добавить проверку bare `.foregroundStyle(.xxx)` паттернов для DarkFantasyTheme токенов.
3. **Починить scheduled task** — обновить путь к скриптам (chronicler вместо hexbound-retro).
4. **Проверить, что все 7 агентов работают** — запустить каждый сканер и убедиться в отсутствии ложных срабатываний.
