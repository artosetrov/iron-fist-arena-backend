# Ретро — 2026-04-06

> **Status boundary:** historical retrospective snapshot. Keep this file as a dated engineering journal entry, not as the current project status or active plan. Revalidate against `wiki/`, live code, and current audit blocks before using it as operational truth.

## Метрики

| Показатель | Значение |
|---|---|
| Коммитов за день | 2 |
| Файлов изменено | 43 |
| Строк добавлено | ~1,076 |
| Строк удалено | ~11,031 |
| Чистый результат | **−9,955 строк** (масштабная чистка) |

### CDO Scan

| Проверка | Результат |
|---|---|
| Invented font tokens | 0 PASS |
| Invented spacing tokens | 0 PASS |
| Hardcoded colors in Views | 1 (CityMapEffects — Color(hex:) extension, не нарушение) |
| Hardcoded system fonts | 0 PASS |
| Raw Color usage | 0 PASS |
| SF Symbol currency | 0 PASS |
| Hardcoded cornerRadius | 2 (1 в комментарии, 1 декоративная 1px полоска — не нарушения) |
| Junk files in xcodeproj | 0 PASS |
| Merge conflict markers | 0 PASS |
| Duplicate "2.swift"/"3.swift" | 0 PASS |

**CDO: CLEAN** — все проверки пройдены.

## Основные изменения

### 1. UnifiedHeroWidget — замена stamina bar на inline display
- Убрана полноширинная Stamina progress bar
- Stamina теперь отображается как `⚡ 85/120` в строке ресурсов рядом с валютами
- Виджет стал ~26px компактнее
- Добавлен `isStaminaLow` (<15%) для danger-цвета
- Добавлен preview "Zero Stamina"
- Figma DS обновлена (оба варианта showCurrencies=true/false)

### 2. Массовая чистка дубликатов (−9,879 строк)
- Удалено **12 дублей** `" 2.swift"` / `" 3.swift"` файлов, случайно добавленных в предыдущем коммите (0400fd8)
- Причина появления: `push and deploy all changes` коммит от 5 апреля закоммитил файлы-дубли из Xcode

### 3. ViewModel improvements
- Множественные ViewModels получили мелкие правки (инициализации, error handling)
- DailyQuestsViewModel, DungeonRushViewModel, InboxViewModel, InventoryViewModel и др.

### 4. Backend — quests daily route
- Правки в `backend/src/app/api/quests/daily/route.ts` и `admin/src/actions/quest-definitions.ts`

### 5. Документация
- Добавлен `docs/09_rules_and_guidelines/INLINE_API_AUDIT.md` (287 строк)

## Сигналы

### 5a. Scanner False Positives
Нет. Все CDO-проверки корректны.

### 5b. Scanner False Negatives
**НАЙДЕН:** Коммит 0400fd8 (5 апреля) случайно добавил 12 файлов-дублей (`" 2.swift"`, `" 3.swift"`). Это ~9,879 строк мусора, который попал в репозиторий через `git add -A`.

**Причина:** Xcode иногда создаёт файлы с суффиксом " 2" при конфликтах сохранения. `git add -A` подхватил их все.

### 5c. Recurring Manual Fixes
Удаление 12 дублей — типичный паттерн. Это уже второй раз, когда дубли Xcode попадают в репо.

### 5d. New Conventions
- Stamina inline pattern (`⚡ value/max` вместо progress bar) — новый UI-паттерн для компактного отображения ресурсов.
- `isStaminaLow` threshold = 15% — для danger feedback.

### 5e. Dead Code
- `staminaBarSection` и `staminaPercent` удалены из UnifiedHeroWidget.

### 5f. Rule Conflicts
Нет.

## Уроки и рекомендации

### Урок 1: Защита от дублей Xcode
Xcode " 2.swift" файлы не должны попадать в репо. Рекомендация: добавить в `.gitignore`:
```
*\ [0-9].swift
*\ [0-9][0-9].swift
```
Или: добавить проверку в preflight скрипт.

### Урок 2: Артефакт hero-widget-prototype.jsx в корне
Файл `hero-widget-prototype.jsx` (420 строк) остался в корне проекта. Это временный прототип, который нужно удалить или переместить в `docs/prototypes/`.

## Правила/агенты обновлены
Нет обновлений правил в этом ретро — уроки зафиксированы как рекомендации.

## Open Items для следующего ретро
1. Проверить, добавлен ли паттерн `" [0-9].swift"` в `.gitignore`
2. Проверить, удалён ли `hero-widget-prototype.jsx` из корня
3. Убедиться, что Figma DS варианты UnifiedHeroWidget синхронизированы с новым inline stamina
