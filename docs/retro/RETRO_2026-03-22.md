# Ретро — 2026-03-22 (воскресенье)

> Финальный прогон (вечерний). 4 коммита за 22 марта + 7 из окна 21 марта.

## Метрики

| Метрика | Значение |
|---|---|
| Коммитов за окно (24ч) | 4 (22 марта) + 7 (в окне 21 марта) |
| Файлов затронуто | 144 |
| Основной объём | iOS: 119, Backend: 12, Agents: 7, Docs: 3, Admin: 1 |

### Текущие нарушения

| Нарушение | Кол-во | Тренд |
|---|---|---|
| Color(hex:) в Views/ | 1 | — |
| Deprecated .foregroundColor() | 12 | 🆕 (впервые отслеживается) |
| Bare tokens (unsafe) | 3 | ↓ (было ~14) |
| Merge conflict markers | 0 | ✅ |
| Prisma schema sync | ✅ | ✅ |
| Junk files (* 2*) | 53 | — |
| accessibilityLabel count | 116 | ↑ |

## Коммиты 22 марта

1. **`cd2a886` feat: ornamental design system, opponent profiles, tutorial, loot rework, and junk cleanup** — мегакоммит: полная ornamental система (OrnamentalStyles.swift + 8 компонентов), opponent profile sheet, tutorial flow, ItemCardView unified refactor, удаление мёртвого кода (HubCharacterCard, CharacterDetailView), loot system rework, battle pass improvements. 143 файла, +8368/−5205.
2. **`952570f` fix(admin): double-cast Prisma Json fields in mail action** — подтвердил правило `as unknown as InputJsonValue` для Prisma Json полей. 2 поля в mail.ts.
3. **`a4663b1` fix(ios): add explicit init to OrnamentalTitle for unlabeled first argument** — SwiftUI struct без explicit init не поддерживает unlabeled first arg.
4. **`38bd758` fix(ios): replace deprecated .foregroundColor() and add accessibility labels** — миграция InboxRowView: `.foregroundColor()` → `.foregroundStyle()` (9 замен). Добавлены accessibilityLabel в LeaderboardPlayerDetailSheet.

## Коммиты 21 марта (из прошлого ретро)

1. **`667387c` chore(release): pre-deploy audit fixes + agent rules update** — 7 агентов созданы
2. **`b3fa40e` chore: stash deleted assets** — массовая чистка + новые компоненты
3. **`14bb557` chore: merge origin/main** — merge с конфликтами
4. **`9857b97` fix: resolve remaining merge conflict markers** — `<<<<<<<` удалены
5. **`42bf9f5` fix(ios): add missing DarkFantasyTheme. prefix** — bare tokens fix
6. **`1de69ce` docs(claude): add merge conflict resolution rules** — новые правила
7. **`eac7471` fix(ios): wrap async closure in Task** — async fix

## Паттерны и уроки

### 1. `.foregroundColor()` — deprecated API, сканер не ловил (НОВОЕ)

**Что:** Commit `38bd758` вручную заменил 9 вызовов `.foregroundColor()` → `.foregroundStyle()` в InboxRowView. Ещё 12 остаются в InboxDetailView.

**Корневая причина:** `.foregroundColor()` deprecated с iOS 17. Guardian scanner не имел проверки — ловил только system colors, но не deprecated modifier.

**Исправлено (этот ретро):**
- Добавлен чек `## Deprecated .foregroundColor()` в `guardian/scripts/check_design_system.sh` (секция 1c)
- Добавлено правило в `guardian/SKILL.md` (секция Common Pitfalls)

### 2. Prisma Json double-cast — правило подтверждено

Commit `952570f` в admin mail.ts. Правило в CLAUDE.md работает корректно.

### 3. Bare Color Tokens — уточнение из прошлого ретро

Все оставшиеся bare tokens покрыты `Color`/`ShapeStyle` extension в DarkFantasyTheme.swift (строки 536-548). Не являются нарушением.

### 4. Merge Conflict — правило подтверждено

Инцидент `14bb557` → `9857b97`. Правило "grep for `<<<<<<<` before commit" в CLAUDE.md + gatekeeper работает.

### 5. hexbound-retro skill создан

Предыдущий ретро отметил, что scheduled task ссылался на несуществующий skill. Теперь `hexbound-retro` существует (commit `cd2a886`).

## Правила/агенты обновлены (этот ретро)

- `guardian/scripts/check_design_system.sh` — добавлена секция 1c: deprecated `.foregroundColor()` detection
- `guardian/SKILL.md` — добавлено правило о deprecated `.foregroundColor()` → `.foregroundStyle()`

## Задачи на следующий ретро

- [ ] Заменить оставшиеся 12 `.foregroundColor()` в `InboxDetailView.swift`
- [ ] Убрать `Color(hex:)` из `LevelUpModalView.swift:200` — заменить на DarkFantasyTheme token
- [ ] Проверить 53 junk файлов (* 2*) — удалить если мусор
- [x] ~~Проверить, что guardian scanner отличает bare tokens с extension от без~~ → уже реализовано (секция 1b сканера)
- [x] ~~Создать skill hexbound-retro~~ → создан в commit `cd2a886`
- [ ] При добавлении новых bare token shorthand — добавлять в extension (строки 536-548 DarkFantasyTheme.swift)
