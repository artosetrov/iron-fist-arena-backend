# Ретро — 2026-04-07 (вторник)

## Метрики

| Метрика | Значение |
|---|---|
| Коммитов за окно | 2 |
| Файлов затронуто | 27 |
| Основной объём | UI polish (UnifiedHeroWidget layout), Shell Game переработка, CityMapEffects атмосферные эффекты |

### CDO Scan

| Проверка | Результат |
|---|---|
| Color(hex:) in Views | 0 PASS |
| Bare theme tokens (unsafe) | 2 (`.textPrimary` без `DarkFantasyTheme.` — legacy в PremiumPurchaseView, VictoryParticlesView, ArenaDetailView, ArenaComparisonSheet, HubEditorDetailView) |
| Merge conflict markers | 0 PASS |
| Prisma schema sync | ✅ PASS |
| Junk files in xcodeproj | 0 PASS |

**CDO: УСЛОВНО CLEAN** — 2 unsafe bare tokens (не новые, legacy).

## Коммиты

1. **`9d8fb47` style(widget): increase name and resource text sizes in UnifiedHeroWidget**
   - Name font: `cardTitle` (18) → `section` (22)
   - Currency/stamina icons: 14px → 18px, text: `uiLabel` → `body bold`
   - Добавлен `.widget` DisplaySize в CurrencyDisplay enum
   - CityMapEffects: добавлены lantern glow и атмосферные эффекты
   - BattleResultCardView: правки
   - `battle-result-audit.html` — новый HTML прототип

2. **`fd21584` style(widget): move resources row below HP bar in UnifiedHeroWidget**
   - Layout: Name → HP bar → Gold/Gems/Stamina (ресурсы больше не конкурируют с именем за горизонт. пространство)
   - Shell Game: масштабная переработка (~653 строк changes) — новый UI, cup glow pulse, result modal
   - ShellGameViewModel: +82 строки (status endpoint, glow state)
   - Backend: `/api/minigames/shell-game/status` — новый endpoint
   - CityMapEffects: замедление анимаций (lantern glow 1.5-3s → 6-10s, wind particles значительно замедлены)
   - NPCGuideWidget, ActiveQuestBanner: мелкие правки
   - FortuneWheelDetailView: правки
   - Новые assets: `npc-shell-master` (@2x, @3x), обновлённый `bg-shell-game`
   - `shell-game-prototype.html` — новый HTML прототип (1409 строк)

## Паттерны и сигналы

### 5a. Scanner False Positives
Нет. Все CDO-проверки корректны.

### 5b. Scanner False Negatives
**Bare `.textPrimary` без `DarkFantasyTheme.` prefix** — scanner показывает 2 unsafe, но реальных случаев больше (6 файлов). Это legacy-код, работающий через Color extension, но формально нарушающий правило "всегда используй полный prefix". Не новое, но не фиксится.

### 5c. Recurring Manual Fixes
**HTML-прототипы в корне проекта** — за последнюю неделю накопилось **7 HTML-файлов** в корне:
- `battle-result-audit.html` (новый сегодня)
- `shell-game-prototype.html` (новый сегодня)
- `hexbound-loader-prototypes.html`
- `fortune-wheel-prototype.html` (6.4MB!)
- `fortune-wheel-v2-prototype.html`
- `dungeon-rush-result-prototype.html`
- `social_buttons_prototype.html`
- `hero-widget-prototype.jsx` (из прошлого ретро, не удалён)

Это не нарушение, но засоряет корень проекта. Все эти файлы — временные прототипы, которые стоит вынести в `docs/prototypes/` или `.gitignore`.

### 5d. New Conventions
1. **CityMapEffects: замедление ambient анимаций** — гасты ветра и свечение фонарей замедлены в 3-5x (с 1.5-3s до 6-10s). Это новый паттерн: "ambient эффекты должны быть медленными и незаметными, а не отвлекающими".
2. **Shell Game result modal** — новый UX-паттерн для минигр: результат показывается в модальном окне вместо inline текста.
3. **`.widget` DisplaySize** — новый размер для CurrencyDisplay (крупнее compact, для виджетов).

### 5e. Dead Code
- `nameAndResourcesRow` рефакторен в `resourcesRow` + inline Text в UnifiedHeroWidget
- `bottomButtonText`, `bottomButtonEnabled` удалены из ShellGameDetailView (UI переработан)
- Старый layout ShellGame (inline cups + bottom button) заменён на новый

### 5f. Rule Conflicts
Нет.

## Open items из предыдущего ретро (2026-04-06)

| Задача | Статус |
|---|---|
| Добавить `" [0-9].swift"` паттерн в `.gitignore` | ❌ Не сделано |
| Удалить `hero-widget-prototype.jsx` из корня | ❌ Не сделано, файл всё ещё на месте |
| Figma DS sync для UnifiedHeroWidget inline stamina | Не проверено |

## Правила/агенты обновлены
Нет обновлений правил — день был focused на UI polish без новых паттернов нарушений.

## Задачи на следующий ретро

1. **Прототипы в корне** — вынести 7 HTML + 1 JSX файлов в `docs/prototypes/` или добавить в `.gitignore`
2. **`.gitignore` для Xcode-дублей** — добавить `*\ [0-9].swift` паттерн (переносится из прошлого ретро)
3. **Bare `.textPrimary`** — решить: либо формально разрешить через Color extension, либо зафиксить 6 мест
4. **`fortune-wheel-prototype.html`** — 6.4MB бинарный HTML в git — удалить или переместить
5. **Ambient animation convention** — документировать правило "ambient эффекты: cycle ≥ 5s" в DEVELOPMENT_RULES.md
