# Ретроспектива — 2026-04-04

> **Status boundary:** historical retrospective snapshot. Keep this file as a dated engineering journal entry, not as the current project status or active plan. Revalidate against `wiki/`, live code, and current audit blocks before using it as operational truth.

## Метрики

| Метрика | Значение |
|---|---|
| Коммиты | 3 |
| Файлов затронуто | 177 |
| Основная область | Hexbound (147 файлов) |
| Новые Swift-компоненты | 5 (ClassTagView, EquippedBadge, GlassStatPill, OpponentStatCell, StatGroupHeader) |
| Новые Dev-каталоги | 5 (BadgesCatalog, CardsCatalog, ComponentsCatalog, ModalsCatalog, ProgressBarsCatalog) |
| Новые скиллы | 7 (ds-code-audit, ds-ecosystem, ds-extract-component, ds-figma-sync, ds-prototype, ds-qa-coverage, ds-screen-builder) |
| Color(hex:) в Views | 0 (чисто) |
| Invented font tokens | 0 (чисто) |
| Invented spacing tokens | 0 (чисто) |
| Prisma sync | ✅ |
| Merge conflict markers | 0 |

## Характер работы

Крупный день дизайн-системного аудита и токенизации. Три коммита — один массивный (172 файла) и два средних fix-коммита:

1. **e6dcafe** — `ds(audit): tokenize fonts, animations, cornerRadius — Sprint 1-3` — массовая замена сырых шрифтов, cornerRadius, анимаций на DS-токены по всему проекту. Удалён `.bak` файл pbxproj. Удалены 12 orphaned sidebar-иконок. Создано 7 новых DS-скиллов. 5 новых reusable-компонентов.
2. **38556dd** — `fix(ds): localize DailyLogin RU→EN, haptic→HapticManager, audit doc update` — локализация, haptic-рефакторинг.
3. **bfec959** — `fix(ds): remove deprecated tokens, fix gems→cyan, update DS docs` — убраны deprecated токены, FortuneWheel доработка.

## Сигналы

### 5a. False Positives сканера
Метрика "Junk files: 69" — **ложное срабатывание**. Скрипт считает файлы с `* 2*` в имени, но в xcodeproj таких junk-файлов нет. Скорее всего паттерн grep ловит что-то в другом месте. Не критично, но метрика вводит в заблуждение.

### 5b. False Negatives (что сканер не ловит)
**Bare `.textPrimary` без `DarkFantasyTheme.`** — найдено 6 мест, где используется `.textPrimary` как bare Color token (через extension), а не `DarkFantasyTheme.textPrimary`. Файлы:
- `PremiumPurchaseView.swift:240`
- `VictoryParticlesView.swift:64, 94`
- `ArenaDetailView.swift:542`
- `ArenaComparisonSheet.swift:440`
- `HubEditorDetailView.swift:232`

Метрика показывает "UNSAFE: 2", но реально 6 мест. Сканер учитывает extension-покрытие, но `.tint(.textPrimary)` всё равно не ideal — лучше явный `DarkFantasyTheme.textPrimary`.

### 5c. Recurring Manual Fixes
Массовая токенизация в e6dcafe — **172 файла** с заменой raw шрифтов/cornerRadius/анимаций на DS-токены. Это повторяющийся паттерн: код писался до появления токенов, потом чистится вручную. Вывод: сканеры уже ловят эти паттерны post-factum, новые нарушения будут пойманы CDO-проверкой.

### 5d. Новые конвенции
- **7 новых DS-скиллов** (`ds-code-audit`, `ds-ecosystem`, `ds-extract-component`, `ds-figma-sync`, `ds-prototype`, `ds-qa-coverage`, `ds-screen-builder`) — полная экосистема для Figma↔Code синхронизации.
- **5 новых reusable-компонентов**: `ClassTagView`, `EquippedBadge`, `GlassStatPill`, `OpponentStatCell`, `StatGroupHeader` — правильный паттерн извлечения из inline в reusable.
- **Dev-каталоги**: 5 каталогов для визуальной проверки DS-компонентов в app.

### 5e. Dead Code
- Удалён `project.pbxproj.bak` (2082 строки) — правильно.
- Удалено 12 orphaned sidebar-иконок (некоторые >1.8MB) — существенная экономия размера бандла.

### 5f. Rule Conflicts / Gaps
**8 audit-файлов в корне проекта** вместо `docs/`:
- `ASSET_CONSISTENCY_AUDIT.md`, `DS_ECOSYSTEM_FULL_AUDIT_2026_04_03.md`, `DS_FIGMA_SYNC_AUDIT_2026_04_03.md`, `HEXBOUND_COMPLETE_SCREEN_AUDIT.md`, `XCODE_ASSET_CATALOG_SCAN.md`, `FULL_PRODUCT_AUDIT_2026-03-30.md`, `HEXBOUND_FIGMA_DESIGN_SYSTEM_TOKENS.md`, `LANDING_ART_PROMPTS.md`

Эти файлы должны быть перемещены в `docs/` или удалены если дублируют. Gatekeeper-скрипт не проверяет наличие audit-файлов в корне.

## Уроки

1. **DS-токенизация завершена на ~95%** — оставшиеся 6 bare token usages и 1 Color(hex:) comment в CityMapEffects — минорные.
2. **Скилловая экосистема для DS зрелая** — 7 новых скиллов покрывают полный lifecycle Figma↔Code.
3. **Audit-файлы в корне** — нужна чистка, gatekeeper должен предупреждать.

## Обновления правил/агентов

Сегодня нет необходимости обновлять правила — все паттерны уже задокументированы в CLAUDE.md. Массовая токенизация была **запланированным sprint**, а не реактивным фиксом.

## Open Items (для следующей ретро)

- [ ] Переместить 8 корневых audit-файлов в `docs/` или удалить дубликаты
- [ ] Проверить 6 bare `.textPrimary` — заменить на `DarkFantasyTheme.textPrimary` или подтвердить extension-покрытие
- [ ] Уточнить метрику "Junk files: 69" в gather_metrics.sh — возможно false positive
- [ ] Проверить что все 5 новых компонентов добавлены в pbxproj (судя по коммиту +20 строк — ок)
- [ ] DEVELOPMENT_RULES.md не обновлялся с 2026-03-21 — может потребовать обновления с учётом DS-экосистемы
